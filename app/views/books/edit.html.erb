<h1>Editing Book</h1>

<%= form_with model:@book, url:books_update_path, local:true do |f| %>
  <P>Title</P>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>


<% end %>

<%= link_to "Show", books_show_path %>
<p>|</p>
<%= link_to "Back", homes_index_path %>